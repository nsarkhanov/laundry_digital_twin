# Production environment variables
# Empty REACT_APP_BACKEND_URL means API calls will use relative paths (/api)
# This allows nginx to proxy requests to the backend
REACT_APP_BACKEND_URL=
ENABLE_HEALTH_CHECK=false
